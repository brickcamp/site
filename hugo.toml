baseURL      = 'https://brick.camp/'
languageCode = 'en-us'
title        = 'BrickCamp'

[caches]
  [caches.getresource]
    dir = ':resourceDir/_req'

[outputs]
  home = ['html', 'rss']
  page = ['html']
  rss = ['rss']
  section = ['html']
  taxonomy = ['html']
  term = ['html']

[uglyURLs]
  _data = true

[taxonomies]
  part = 'parts'
  size = 'size'
  uses = 'uses'
  tag  = 'tags'