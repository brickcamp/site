baseURL      = 'https://brick.camp/'
languageCode = 'en-us'
title        = 'BrickCamp'

[build]
  [build.buildStats]
    enable = true

[caches]
  [caches.getresource]
    dir = ':resourceDir/_req'

[outputs]
  home = ['html', 'rss']
  page = ['html']
  rss = ['rss']
  section = ['html']
  taxonomy = ['html']
  term = ['html']

[taxonomies]
  part = 'parts'
  size = 'size'
  uses = 'uses'
  tag  = 'tags'

[uglyURLs]
  _data = true